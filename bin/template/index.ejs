import useMount from "@/use/use-mount";
import Page from "@/components/uiPage/UIPage";
import CacheHoc from "@/containers/CacheHoc";
import PropTypes from "prop-types";

import "./<%=pageName%>-container.less";

const CONST_CACHEID = "<%=pageName%>";

const <%=className%> = ({leavePage, cacheState}) => {

    useMount(() => {
        // 页面挂载
    }, () => {
        // 页面销毁
    })

    return (
        <Page
            title=""
            className="<%=pageName%>-container"
            routeName="<%=pageName%>"
            outLint={true}
        >
            <div className="<%=pageName%>-body">
                {/* 页面内容 */}
            </div>
        </Page>
    )
}

<%=className%>.propTypes = {
    leavePage: PropTypes.func,
    cacheState: PropTypes.object || null
}

export default CacheHoc(<%=className%>, {
    cacheId: CONST_CACHEID
});